pipeline{
    agent any
    stages {
        stage('checkout') {
                steps{
                    checkout scm
                }
            }
            stage('build') {
                steps {
                    sh 'echo "build step(if needed) done"'
            }
        }
        stage('deploy') {
            steps {
                withAWS(region: 'ap-south-1', credentials: '-baac-a') {
                }
            }
        }
    }
}  
